# Week 3 data

Place private training images under `training/known/` and `training/unknown/` when training locally.

Runtime event data is stored in `events.db` and visitor snapshots are stored in `snapshots/`. These paths are ignored by Git.

Do not commit real visitor photographs or biometric datasets to a public repository.
