# Trained model

`train_classifier.py` writes the trained visitor-classification pipeline here as `visitor_classifier.joblib`.

The generated model is intentionally ignored by Git because it may encode information derived from private biometric training images.
